name: TERRAFORM PROJ

on:
  push:
   # branche: [ master ]
   

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v1
    - name: Build with Maven
      run: mvn package
